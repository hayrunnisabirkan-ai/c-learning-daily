#include <stdio.h>
#include <stdlib.h>

int main() {

/*  Farklı data typeler için (char int float double) değişken tanımlayıp 
değer ataması yapınız hem bu değerleri hem de bellekte tuttukları adresi
console a  bastırınız. */ 

 char character='C';
 int integernumber=1;
 float floatnumber=5.66;
 double doublenumber=234536600;

 printf("Character = %c, Character of adress =%d\n", character, &character);
 printf("integernumber = %c, integer of adress =%d\n", character, &character);
 printf("Character = %c, Character of adress =%d\n", character, &character);
 printf("Character = %c, Character of adress =%d\n", character, &character);
}